#include <stdio.h>

int main()
{

enum fuzzy_logic{ false, maybe, true};
//false ki value 0 and so on....................
///enumanator me indexing 0 se start hoti hai;
printf("%d",fuzzy_logic(1));
return 0;
}
